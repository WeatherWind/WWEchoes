# WWEchoes-Tool — 仓库协作说明

《鸣潮》(Wuthering Waves) Windows 桌面小工具：玩家进入游戏内「角色-声骸装配」页面时，自动识别当前角色与所穿声骸，实时给出声骸评分，并以透明悬浮窗显示在游戏上方。**从零开始的项目**（2026-09-18 时仅有 git init，尚无代码、无远程仓库）。

## 硬性合规红线（任何方案与代码不得触碰）
- 不读游戏内存、不注入游戏进程、不修改游戏文件、**不模拟键鼠输入**（保持"只读截图"定位，主动收窄反作弊风险面）。
- 允许的外部感知手段仅限：窗口/进程枚举、屏幕或窗口截图（WGC/DXGI 等）、OCR/图像识别。
- 仅支持 Windows；目标用户为普通玩家，最终需可打包分发。

## 产品行为要点
- 悬浮窗：透明、置顶、点击穿透、不抢焦点、不进任务栏，完全不干扰游戏操作。
- 仅在玩家停留在「角色-声骸装配」页面时唤醒显示，离开后隐藏——需画面检测 + 去抖 + 前台校验（勿用窗口枚举判断游戏内页面，参考调研文档的状态机范式）。
- 游戏进程名：`Client-Win64-Shipping.exe`；识别假设 16:9 分辨率；需处理 DPI 缩放（PerMonitorV2）。
- 游戏须以无边框窗口/窗口化运行（独占全屏下悬浮窗与捕获都会失效），工具内需明示用户。

## 技术调研结论（详见 docs/research/2026-09-18-参考项目与技术方案调研.md）
- 悬浮窗标准配方（两个参考项目实证）：原生 Win32 窗口 + `WS_POPUP | WS_EX_TOPMOST | WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW` + `SW_SHOWNOACTIVATE`；悬浮窗需要交互时用 `WH_MOUSE_LL`/`WH_KEYBOARD_LL` 低级钩子按区域拦截。
- 画面捕获首选 WGC（windows-capture 库），备选 DXGI（dxcam）；BitBlt/PrintWindow 仅作回退（Win11 需注册表 hack）。
- 页面检测：截屏 + 固定 ROI 模板/像素匹配 + 去抖状态机；文字识别首选 PP-OCR 的 ONNX 形态（RapidOCR）CPU 推理。
- 最接近的同类项目是 SwordSong/MCEchoSys（游戏内实时声骸识别 + PyQt6 悬浮窗，极早期）；其余同类工具均为离线评分或库街区数据导入——**本产品定位在开源社区没有直接竞品**。
- 性能注意：覆盖窗常驻可见会强制游戏走 DWM 合成（帧节奏开销），应"内容不变不重绘、无内容 SW_HIDE"。

## License（已决策，见 docs/adr/0002）
- 本项目采用 **GPL-3.0**，并复用 WutheringWavesUID 的评分公式与词条数值表（GPL 兼容）。
- 两个参考项目（WWMAP-TOOLS、IMao-Wuthering-Waves）同为 GPL-3.0，可按需引用其代码，README 标注来源。
- 仓库计划公开开源（GitHub + Release 分发）。

## 已定关键决策（详见 docs/adr/ 与 docs/worklog/2026-09-18-需求对齐-grilling.md）
- 技术栈：Python 3.11+ / PySide6（docs/adr/0001）；打包走 GitHub Actions Windows runner，分发型态：**单 exe（onefile）**（用户选定）。
- 评分：词条权重打分（WWUID 口径 250 分制 SSS~C + 有效词条着色）；形态：**详情联动 + 汇总条**（切角色重置）。
- 悬浮窗：纯展示（全穿透/不抢焦点/固定角落可配置，默认右上）；托盘 + 全局热键（仅监听）控制显隐。
- 范围：v1 仅国服简中、16:9 以 1920×1080 为基准；库街区数据 v1 不引入。
- 角色识别兜底：手动选 + 记住上次。产品名：WWEchoes。
- 合规：只读截图、不模拟输入（docs/adr/0003，刻意收紧，勿"顺手"放宽）。

## 目录与文档约定
- `CONTEXT.md` — 领域术语表（glossary）。讨论与代码命名须与术语一致；新术语确定后随手更新。
- `docs/adr/` — 架构决策记录（改这些决策前先读对应 ADR）。
- `docs/research/` — 调研报告。结论必须附来源；"未确认"标注是诚实边界的一部分，不得删除。
- `docs/worklog/` — 完工日志（`YYYY-MM-DD-<主题>.md`），每次实质性工作后更新（全局协作约定要求）。
- `docs/assets/` — 游戏截图等素材（用户提供的 1080p 国服装配页/详情面板截图将放此处，用于 ROI 设计）。
- 改动合规红线、悬浮窗 Win32 样式、评分数据/算法前，先读 `docs/research/2026-09-18-参考项目与技术方案调研.md` 对应章节。

## 版本控制
- 本地 git（master），**尚无远程仓库**。用户明确要求做好版本控制：实质变更须提交，提交信息说明动机；建立远程仓库后更新本节。

## 协作模式（双机跨对话）
- **macOS 会话**：纯逻辑（评分引擎、词条数据、ROI 计算）、UI 骨架、文档、CI 配置；本地可跑单测。
- **Windows 会话**：截屏（WGC）、悬浮窗 Win32 样式实测、ROI/模板调参（配合 `docs/assets/` 里的 1080p 截图）、打包验证；用户提供截图在该会话进行。
- **同步机制：远程仓库（待建，优先级最高的前置项）**；两边会话开始工作前先 `git pull`，结束前提交推送。
- Windows 会话的 agent 必须先读本文件与 `CONTEXT.md`；合规红线（ADR-0003）对两个环境同样生效。
- 平台守卫：Windows 专用代码集中在 `capture/`、`overlay/` 的 win 模块内，用懒加载隔离，保证 mac 上可导入运行其余部分与单测。

## 当前状态与下一步
- 已完成：初始技术调研、需求对齐（14 项决策收敛）、CONTEXT.md、3 个 ADR、M0 项目骨架与评分引擎。
- 进行中：评分引擎移植与单测（mac 侧）；等待 Windows 会话接手 capture/overlay/ROI 实测。
- 待用户动作：① 建远程仓库（或授权 `gh` 创建）——双机同步的前置；② Windows 会话提供 1080p 国服截图到 `docs/assets/`。
- 远程仓库建立后：接入 GitHub Actions 打包并更新本节。
