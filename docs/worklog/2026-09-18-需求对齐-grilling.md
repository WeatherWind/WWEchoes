# 完工日志：需求对齐 grilling 会话（2026-09-18）

## 本次工作内容
- 用 grill-with-docs（grilling + domain-modeling）完成需求对齐：3 轮结构化提问（harness AskUserQuestion），14 项决策全部收敛，前沿清空。
- 落盘领域术语表 `CONTEXT.md`（9 个术语）与 3 个 ADR；更新 `AGENTS.md`（License、既定决策、目录约定、当前状态）。

## 决策清单（用户已拍板）
| # | 决策项 | 结论 |
|---|---|---|
| 1 | 技术栈 | Python 3.11+ / PySide6（ADR-0001） |
| 2 | License | GPL-3.0，复用 WWUID 评分公式+数值表（ADR-0002） |
| 3 | 评分模型 | 词条权重打分（250 分制 SSS~C + 有效词条着色） |
| 4 | 评分形态 | 详情联动 + 汇总条（5 槽位，切角色重置） |
| 5 | 悬浮窗交互 | 纯展示（全穿透/不抢焦点），托盘+全局热键显隐 |
| 6 | 客户端范围 | 仅国服简中 |
| 7 | 分辨率 | 仅 16:9，1920×1080 基准；游戏须无边框窗口 |
| 8 | 库街区 | v1 不引入 |
| 9 | 分发形态 | **单 exe（onefile）**——用户自选，非推荐项（已知代价：启动解压慢、误报率更高） |
| 10 | 仓库 | 公开开源（GitHub + Release） |
| 11 | 角色识别兜底 | 手动选 + 记住上次 |
| 12 | 悬浮窗位置 | 固定角落可配置（默认右上） |
| 13 | 截图素材 | 用户将提供 1080p 国服 PNG 截图（装配页+详情面板） |
| 14 | 产品名 | WWEchoes |

## 由决策推导的既定安排（非用户直接拍板，文档记录在案）
- 打包：macOS 无法交叉打包 PyInstaller → GitHub Actions Windows runner 构建，Release 即测试包。
- 合规加严：不模拟键鼠输入列入红线（ADR-0003），悬浮窗因此纯展示。
- 默认性能目标（可在实测后调整）：详情面板出现→出分 ≤1s；页面状态检测周期 100–200ms；悬浮窗内容不变不重绘。

## 完成边界
- **做了**：需求决策、术语表、ADR、AGENTS.md 更新、本日志、git 提交。
- **没做**：任何代码。grilling 流程要求用户确认共识后才启动实现。
- **待用户动作**：① 确认共识（见会话中的共识摘要）；② 提供 1080p 国服截图（ROI 设计前置）；③ 建远程仓库（或授权我用 gh 创建后接入 CI）。

## 变更清单
- 新增：`CONTEXT.md`、`docs/adr/0001-python-pyside6-技术栈.md`、`docs/adr/0002-gpl3-与-wwuid-评分复用.md`、`docs/adr/0003-只读截图合规边界.md`、本日志。
- 修改：`AGENTS.md`（License/既定决策/目录约定/当前状态四节）。
